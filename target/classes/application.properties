spring.application.name=CareCompare_App
spring.datasource.url=jdbc:postgresql://localhost:5432/carecompare_db
spring.datasource.username=postgres
spring.datasource.password=SweRee@123
spring.datasource.driver-class-name=org.postgresql.Driver

# Ensure schema.sql runs on startup
spring.sql.init.mode=always

# Hibernate settings (optional but recommended)
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Add Secure JWT Secret Key (At least 32 characters)
jwt.secret=YJks38ds09skS9dq0382asShNksdm923MSD8asdlkq02893sadasdjklqw82
jwt.expiration=86400000  # 1 day in milliseconds

# Enable Actuator
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always
